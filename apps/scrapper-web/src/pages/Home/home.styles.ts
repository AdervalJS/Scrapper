import styled from 'styled-components';
import * as template from '../../template/home';

export const Container = styled(template.Container)``;

export const Carousel = styled(template.Carousel)``;

export const Reading = styled(template.Reading)``;

export const Updates = styled(template.Updates)``;

export const FeaturedTitle = styled(template.FeaturedTitle)``;

export const Featured = styled(template.Featured)``;

export const Banner = styled(template.Banner)``;
