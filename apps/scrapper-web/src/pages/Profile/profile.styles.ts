import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  top: 0px;
  left: 0px;
  right: 0px;
  height: 100vh;
  z-index: 100;

  padding: 30px;

  backdrop-filter: blur(20px);
`;
